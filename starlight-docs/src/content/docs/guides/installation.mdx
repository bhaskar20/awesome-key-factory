---
title: Installation
description: Install React Query Key Factory in your project.
---

## Using Yarn (Recommended)

```bash
yarn add react-query-key-factory
```

## Using npm

```bash
npm install react-query-key-factory
```

## Peer Dependencies

This library requires TypeScript version 4.0.0 or higher.

Make sure you have TypeScript installed in your project:

```bash
yarn add -D typescript
# or
npm install --save-dev typescript
```
